apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-users
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-users
  template:
    metadata:
      labels:
        app: mongo-users
    spec:
      containers:
      - name: mongo
        image: mongo:6
        ports:
        - containerPort: 27017
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-users
spec:
  selector:
    app: mongo-users
  ports:
  - port: 27017
    targetPort: 27017
  type: ClusterIP