apiVersion: v1
kind: Service
metadata:
  name: service-users
spec:
  selector:
    app: service-users      # cible les pods avec ce label
  ports:
  - port: 3001
    targetPort: 3001
  type: ClusterIP           # accessible uniquement dans le cluster